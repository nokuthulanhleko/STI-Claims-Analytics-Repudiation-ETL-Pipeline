--- Power Query (M) snippets to load reporting views
-- Replace SERVER_NAME and DATABASE_NAME with your values

/* vw_claims_summary */
let
    Source = Sql.Database("SERVER_NAME", "DATABASE_NAME", [Query="SELECT * FROM dbo.vw_claims_summary;"])
in
    Source


/* vw_driver_eligibility_analysis */
let
    Source = Sql.Database("SERVER_NAME", "DATABASE_NAME", [Query="SELECT * FROM dbo.vw_driver_eligibility_analysis;"])
in
    Source


/* vw_fraud_analysis */
let
    Source = Sql.Database("SERVER_NAME", "DATABASE_NAME", [Query="SELECT * FROM dbo.vw_fraud_analysis;"])
in
    Source


/* vw_monthly_claim_trends */
let
    Source = Sql.Database("SERVER_NAME", "DATABASE_NAME", [Query="SELECT * FROM dbo.vw_monthly_claim_trends;"])
in
    Source


/* vw_repudiation_analysis */
let
    Source = Sql.Database("SERVER_NAME", "DATABASE_NAME", [Query="SELECT * FROM dbo.vw_repudiation_analysis;"])
in
    Source

-- Usage: In Power BI Desktop, choose Get Data → Blank Query → Advanced Editor and paste the relevant block.
